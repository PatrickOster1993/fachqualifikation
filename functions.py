# Funtkion zum Addieren 
def addiere(a, b):
    ergebnis = a + b
    return ergebnis

x = int(input("Zahl 1: "))
y = int(input("Zahl 2: "))

print(f"Ergebnis der Addition: {addiere(x, y)}") # Ausführung der Funktion
